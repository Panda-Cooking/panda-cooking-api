import { iRecipeRequest } from "../../interfaces/recipes/recipesInterface";

const createRecipeService = async (
    userAuthId: string,
    newRecipe: iRecipeRequest
) => {};

export default createRecipeService;
